using Microsoft.AspNetCore.Authentication;

namespace weesky.Scotty.Microservice.Authentication.Dav;

/// <summary>No option of its own: everything this scheme needs is injected or configured elsewhere.</summary>
public sealed class DavAuthenticationOptions : AuthenticationSchemeOptions;
